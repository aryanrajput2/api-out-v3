import requests
from config import API_KEY, BASE_URL


def cancel_booking(data):

    url = f"{BASE_URL}/booking/cancel"

    headers = {
        "Content-Type": "application/json",
        "apikey": API_KEY
    }

    response = requests.post(url, headers=headers, json=data)

    return response.json()